name: familychat_flutter
description: "Closed family chat rebuilt with Flutter Web."
publish_to: 'none'
version: 1.0.18+19

environment:
  sdk: ^3.11.3

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  file_picker: ^10.3.3
  intl: ^0.20.2
  shared_preferences: ^2.5.3
  supabase_flutter: ^2.10.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
